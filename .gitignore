# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
/dist/
build/
*.egg
.venv/
venv/
.pytest_cache/

# Node
node_modules/
# NOTE: frontend/dist IS committed on purpose — the Windows lab host runs the
# GUI without Node/npm (see docs/windows_smoke_test.md). After changing the
# frontend, run `npm run build` and commit the updated dist.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local

# Data (large files)
*.npy
*.hdf5

# Claude Code agent worktrees + local preview config
.claude/worktrees/
.claude/launch.json

# Acquisition output (dev-mode bridge writes here)
data/
